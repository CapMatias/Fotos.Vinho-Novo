# Copie este arquivo para ".env" e preencha. O .env fica só no seu computador
# (já está no .gitignore) e serve para testar antes de publicar.
# As mesmas variáveis vão na Netlify em Site configuration > Environment variables.

# ---- banco (obrigatório) ----
SUPABASE_URL=https://onzkifuftbslbaitvnqc.supabase.co
SUPABASE_SERVICE_KEY=cole-aqui-a-chave-service_role

# ---- chave do painel (obrigatório) ----
PAINEL_KEY=escolha-uma-palavra-secreta-comprida

# ---- onde guardar as fotos ----
# Com estas três preenchidas, as fotos vão para o Cloudinary (25 GB grátis).
# Deixe em branco para usar o Supabase Storage (1 GB).
CLOUDINARY_CLOUD_NAME=nhusipwg
CLOUDINARY_API_KEY=819269837485622
CLOUDINARY_API_SECRET=LqiU-cwBJTuDbJvV4gF05hi4XK4
